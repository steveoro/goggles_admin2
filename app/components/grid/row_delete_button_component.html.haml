= link_to({ controller: @controller_name, action: :destroy, params: { id: @asset_row.id,
            page: params[:page], per_page: params[:per_page] } },
            { id: "frm-delete-row-#{@asset_row.id}",
            class: 'btn btn-sm btn-outline-danger mb-1',
            data: { turbo_method: :delete, turbo_confirm: t('dashboard.confirm_row_delete',
            label: @asset_row.send(@label_method || 'id')) } }) do
  %i.fa.fa-trash-o
