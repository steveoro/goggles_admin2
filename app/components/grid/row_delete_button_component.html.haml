= link_to({ controller: @controller_name, action: :destroy, params: { id: @asset_row.id } },
            { id: "frm-delete-row-#{@asset_row.id}", method: :delete,
            class: 'btn btn-sm btn-outline-danger',
            data: { confirm: t('dashboard.confirm_row_delete',
            label: @asset_row.send(@label_method || 'id')) } }) do
  %i.fa.fa-trash-o
