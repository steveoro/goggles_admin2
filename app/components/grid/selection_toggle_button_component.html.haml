%button.btn.btn-outline-primary.btn-sm{ 'aria-controls' => 'select-toggle', type: 'button',
        data: { toggle: 'tooltip', title: I18n.t('dashboard.toggle_selection') },
        onclick: "document.querySelectorAll('input[type=checkbox].grid-selector').forEach(function(el) { el.click(); });" }
  %i.fa.fa-check-square-o
