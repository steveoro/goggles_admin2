- if @param_name.present?
  = paginate(Kaminari.paginate_array(@data, total_count: @total_count).page(@page).per(@per_page), param_name: @param_name)
- else
  = paginate(Kaminari.paginate_array(@data, total_count: @total_count).page(@page).per(@per_page))
