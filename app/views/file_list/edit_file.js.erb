document.querySelector('#flash-messages-container').innerHTML = "<%=
  escape_javascript(
    render(partial: 'goggles/flash_messages')
  )
%>";

document.querySelector('#file-modal').innerHTML = "<%=
  escape_javascript(
    render(
      partial: 'file_modal',
      locals: {
        file_path: @file_path,
        file_content: @file_content,
        parsed_json: @parsed_json,
        rename: false
      }
    )
  )
%>";
$('#file-actions-modal').modal();
