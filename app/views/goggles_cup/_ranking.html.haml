-# Delegates to the shared engine ranking partial. The engine partial expects
-# :ranking_data, :team, :goggle_cup, :no_duplicated_events and optional :show_exports.
= render partial: 'goggles_db/goggle_cups/ranking', locals: local_assigns
